# .gcloudignore — Controls what gets uploaded to Cloud Build
# This OVERRIDES .gitignore for cloud deployments

node_modules/
dist/
.env
*.log
inspect-db.js
README.md
SQL_SCHEMA.sql

# NOTE: service-account.json is intentionally included
# so Cloud Build can embed it in the Docker image
